import 'package:password/Contracts%20(%20interfaces%20)/vault_repo.dart';

class VaultService {
  VaultRepo vr;
  VaultService(this.vr);

  // Future<List<int>> generateSalt();
  // Future<bool> unlockVault(String masterpassword);
  // Future<bool> lockVault();
}
